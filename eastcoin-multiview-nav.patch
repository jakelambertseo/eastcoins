diff --git a/assets/eastcoins-ux.js b/assets/eastcoins-ux.js
--- a/assets/eastcoins-ux.js
+++ b/assets/eastcoins-ux.js
@@ -2039,5 +2039,46 @@
     if (event.key === KEYS.soundEnabled) {
       sendSettingsToFrames();
     }
   });
 })();
+
+/* ---------------- SITEWIDE MULTIVIEW NAV ---------------- */
+(() => {
+  "use strict";
+
+  document.querySelectorAll(".nav-group").forEach((nav) => {
+    if (nav.querySelector('a[href*="multiview.html"]')) {
+      return;
+    }
+
+    const link = document.createElement("a");
+    link.className = "nav-link";
+    link.href = "multiview.html";
+    link.target = "_top";
+    link.textContent = "▦ MultiView";
+
+    const eventsLink = Array.from(
+      nav.querySelectorAll("a.nav-link")
+    ).find((candidate) => {
+      try {
+        return new URL(
+          candidate.href,
+          window.location.href
+        ).pathname
+          .toLowerCase()
+          .endsWith("/events.html");
+      } catch {
+        return false;
+      }
+    });
+
+    if (eventsLink) {
+      eventsLink.insertAdjacentElement(
+        "afterend",
+        link
+      );
+    } else {
+      nav.appendChild(link);
+    }
+  });
+})();
